The folders contain outlier prone datasets with the 4 different kinds of
outlier policies -- random errors, locally clustered, randomly
grouped and locally grouped. Each dataset contains the scaling and
weight parameters embedded in the name. 
